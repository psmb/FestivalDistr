prototype(S:VenuePage) {
	templatePath = 'resource://Sfi.Site/Private/TypoScript/NodeTypes/VenuePage.html'
	main = TYPO3.Neos:ContentCollection {
		nodePath = 'main'
	}
	articles = TYPO3.Neos:ContentCollection {
		nodePath = 'articles'
		attributes.class = 'row small-up-1 medium-up-2 large-up-3'
	}
	# Dirty hack
	showArticles = ${documentNode.context.inBackend || q(node).children('articles').children().count() > 0 ? true : false}
	venuesMenu = S:VenuesMenu
}
